#include <bits/stdc++.h>

using namespace std;

int main()
{
    // open disjoint.in
    ifstream f("disjoint.in");
    // open disjoint.out
    ofstream g("disjoint.out");
    g << "gg";

    // close disjoint.in
    f.close();
    // close disjoint.out
    g.close();
    return 0;
}